# Changelog — продакшн Firebase конфіг

## Що зроблено
- Заповнено `firebaseConfig` у `public/index.html` бойовими значеннями (apiKey, authDomain, projectId, storageBucket, messagingSenderId, appId, measurementId) згідно з наданими даними.
- Встановлено `ADMIN_UIDS` на наданий UID для гейтингу адмін-UI.

## Як було
- У `public/index.html` залишалися плейсхолдери `YOUR_*` у `firebaseConfig` та `ADMIN_UIDS`.

## Як стало
- `firebaseConfig` містить реальні параметри проєкту `sunfixer-website` та measurementId.
- `ADMIN_UIDS` містить UID `CA450nemojZFKUZ8rmtZmXEqMeF3`.

## Причина змін
- Перехід із мок-конфігурації до бойової для підключення реальної Firebase (Auth/Firestore/Storage) та активації адмін-режиму.
