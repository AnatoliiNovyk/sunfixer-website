# Зміни 2025-12-17

- Додано окреме зображення hero з градієнтним плейсхолдером; тепер hero-фото показується через `<img id="heroImage">`, а плейсхолдер ховається після успішного завантаження.
- Потік `artist_info` тепер конвертує `hero_image` (gs://) у HTTPS через `getDownloadUrlIfGs`, тож немає 404/CORS при відображенні.
- Після аплоаду з адмінки hero одразу рендериться локальним `downloadURL`, без очікування повторного читання з Firestore.
- Додано тестовий сценарій `tests/hero-image-upload.md` для перевірки аплоаду/рендеру hero-фото.
- Причина: виправити головну проблему — зображення з адмінки не відображалось і спричиняло CORS/404, бо зберігалось лише як `gs://` без конвертації та рендеру.
